# LearnNote 0.2.0 工作台重设计

## 目标与边界

本轮针对用户明确提出的界面杂乱、操作不连贯和笔记质量问题，重做默认应用入口、官网与 README。保留本地数据格式、媒体获取、转写、证据和打包底层；不把重写界面理解为清空用户资料。

默认页面不再加载旧版 app.js 和六层应用 CSS。它使用独立的 desk.js、desk-api.js 与 desk.css。课程归档、批量链接、范围学习、OCR 审阅、提问、复习卡、修订稿多格式导出、补充管理、存储、备份与诊断已迁入统一工具面板。classic.html 和旧主脚本仅留作源码中的历史回归，安装包明确排除这些文件，并设有发布检查。

## 主流程

- 一个新建入口：链接、本地文件或浏览器交接；整理偏好默认折叠。
- 左侧笔记列表，中间正文，按需打开原始来源。
- 视频时间戳和字幕点击定位；播放时高亮对应字幕。
- 直接编辑 Markdown，修改独立保存，原始笔记与来源证据不被覆盖。
- 乐观修订检查拒绝过期保存；离开时提示未保存正文或补充。
- 正文补充、Markdown 导出、基础复习、模型设置与深浅主题。
- 对已有视频可重新整理，生成新任务，保留旧笔记及修改。

## 内容整理

默认本地降级路径改为明确标记的字幕摘录，按时间和文本长度分段，只出现一遍原文；不再将同一句话复制到核心概念、易错点和复习等模板。专业旧模板保留兼容行为。

默认模型提示改为内容命名的主题结构，不要求最低字数、固定题数或统一空章节。文字路径使用带时间戳的字幕块，长输入逐块处理，某块失败时整体降级到完整摘录，不把缺失后半段的结果当作完整笔记。视觉合并路径仍需更大的真实材料基准。

这些改变消除了已知模板问题和首段截断，不代表已经通过真实模型准确率或学习效果评估。当前回归采用可复现合成材料和模拟模型；不将其展示成真实课程效果。

## 官网与说明

官网采用与应用一致的浅色阅读风格，移除重装饰和功能指标堆叠。效果区域明确为演示内容。所有下载入口指向 GitHub 实际 latest release，避免候选版尚未发布时链接到不存在的安装器。

README 按快速开始、输入路径、生成结果、隐私边界和开发运行组织。手机仅提供基本窄屏可用性，不承诺手机 App 或跨设备同步。新工作台当前是中文预览，完整双语仍在旧工具页；对应检查分别验证实际页面。

## 验证入口

- 后端：`python -m unittest discover backend/tests`
- 脚本：`python -m unittest discover scripts/tests`
- 新工作台真实交互：`node scripts/redesign-acceptance.cjs http://127.0.0.1:8765 build/redesign-ui`
- 官网：`node scripts/site-visual-acceptance.cjs http://127.0.0.1:18781 build/redesign-site`
- 旧工具页继续运行原有合约、无障碍和学习流程测试。

本分支为重设计预览。签名、安装器覆盖升级、真实登录站点与真实远程模型质量验收不因界面测试通过而被视为完成。

## 本次验收结果

- 后端完整回归 447 项通过；脚本 57 项、桌面启动 24 项通过。
- 新工作台浏览器验收通过长文完整阅读、修订保存和刷新、原文不被覆盖、补充保存、深浅主题及 390px 布局。
- 官网 1440px / 390px 布局、下载与隐私导航通过。
- 实际 PyInstaller Windows 0.2.0 / WebView2 窗口通过笔记阅读、视频时间跳转和字幕高亮、复习评分、模型设置显示；本地 ASR 组件可用。
- 发布目录审计通过；演示数据位于包外的隔离目录。
- 新的 UI 合约与旧工具页合约分别保留；源码工作台截图使用标明性质的演示内容。


## 0.2.1 统一工作台

- 顶部只保留编辑、查看来源、更多；专项操作在同一套工具面板中完成。
- 课程支持创建、编辑、排序、暂停、批量链接、播放列表预览和引用对照；课程分组删除不删除原资料。
- 复习支持从当前来源提出卡片、编辑确认、计划设置、课程过滤和评分历史。
- 片段学习与可选 OCR 复用缓存媒体；OCR 原帧和置信度在同一阅读环境显示。
- Markdown、DOCX、PDF 使用保存后的修订稿，可附上个人补充；原始生成稿导出单独标记。
- 文档提问只检索当前文档并返回原文，明确不作推断，不跨文档混入无关内容。
- 新增存储检查、索引备份与恢复、清理预览、处理诊断和脱敏支持包入口。
- 索引备份范围明确为任务索引，不冒充含所有媒体和个人资料的完整备份。
- 原始旧界面不再打包；历史源码的测试继续独立运行。


### 0.2.1 验收

后端 452 项、脚本 57 项、桌面启动 24 项通过；Web 9 个测试文件和扩展 47 个测试文件通过。全新打包数据目录下完成课程、复习卡、文档问答、个人补充编辑、计划、索引备份和 Markdown / DOCX / PDF 修订稿导出。另以带字幕的合成视频验证片段任务、本地 OCR 和原帧显示。默认复习计划的首次并发创建用强制并发测试验证。

实际 Windows EXE 版本为 0.2.1；发布目录审计确认旧界面文件缺席，旧页面、旧主脚本与旧样式的 HTTP 请求返回 404。数据目录与演示任务不进入 ZIP。模型效果、真实登录站点、签名和安装器升级仍不属于此交互验收的证明范围。
