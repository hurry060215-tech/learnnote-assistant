# 需要用户完成的外部事项

本文件只列开发与自动化无法代替的条件；代码、测试、文档和符合条件的 PR 合并继续由代理处理。商店仅为 Google Chrome Web Store。

1. **Google 现有条目**：README 和官网已有条目 ID `mncdchpkpikhacmkbanedpppcddapppe`，连接文档也记录过提交审核；不要求重新注册或新建条目。请在 Developer Dashboard 确认当前审核/发布状态、商店版本和 publisher ID。若要由工作流上传，请将 access token 等配置到仓库 Secrets，不发到聊天。当前网络工具无法核验公开 listing 状态，不能据此断言条目不存在。实际上传/审核及正式公开仍按原约定单独授权。
2. **Chrome 原生权限操作**：需要在独立测试配置执行允许、拒绝、撤销、重新授权，并验证重新启动后状态。上次 Windows 电脑操作工具因无法识别浏览器 URL 而停止；自动化竞态测试已通过，但不能代替原生弹窗验收。此项不阻塞后端工作。
3. **真实来源已完成一次完整运行**：BV1kbb56DEkM 的 38 分 31 秒视频已下载、本地转写并生成 DS 笔记；不用重新提供视频。数字、音译和推断仍需内容核对，不能把生成成功当作事实全部验证。原生站点授权矩阵仍需用户操作，不需要发送 Cookie、密码或验证码。
4. **模型已确定并实测**：使用既有 deepseek-flash，生成阶段 2 次请求共 39,819 tokens；独立连通性探测另 23 tokens。密钥仅在进程内使用。无需再次提供模型名或密钥。大规模付费基准的费用上限需另行确定。
5. **Word/WPS**：请打开 `build/long-export-20260920/long-note.docx` 和 `long-note.pdf`，检查分页、字体、表格、代码与来源链接，反馈软件版本和异常页码。PDF 为 35 页，结构检查通过；DOCX 自动渲染明确失败于缺少 LibreOffice soffice.exe。该样例只覆盖文本、表格、代码、Unicode 简单公式和链接，不代表图片/复杂可编辑公式已完整验收。
6. **macOS**：提供可用 Mac 或由你按验收清单执行原生安装、凭据保存、迁移、升级回退与扩展连接。当前 Windows 主机不能提供 macOS 实机证明。签名/公证需要相应 Apple 开发者条件。
7. **Windows 签名与正式发布**：若要求受信任签名，需要证书或签名服务；通过安全凭据配置提供，勿发送证书密码。未具备前维持未签名及校验和说明。正式发布另行授权。

目前不需要你手工删分支、删数据或整理缓存；所有现有配置、课程资料、登录态和历史证据继续保留。
