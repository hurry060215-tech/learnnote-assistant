# 第 2–3 批修复与受控验收

执行日期：2026-09-20。PR #190 已合并为 e8b16b8，替代的 #186 已关闭。

## 已落地

- 第 2 批提交 15abacd：发布打包前必须执行 scripts/reliability-suite.py。矩阵执行失败、输出报告缺失、报告状态失败或运行期间源码改变均阻断后续打包。报告包含 Git SHA、Python、实际安装依赖、命令、退出码和子报告 SHA-256。失败证据通过 always() 上传。
- 5/30/60/180 分钟媒体输入分别执行抽帧资源门禁和完整任务管线。完整任务使用固定字幕与离线摘要 fixture，不执行真实 ASR 或远程模型；资源门禁限制的是父 Python 进程 RSS，不能代替进程树峰值实测。
- 修正超过一小时的合成 SRT 时间码，分钟字段保持 00–59。
- 第 3 批提交 4079c2c：撤销站点权限清除已完成 B 站字幕缓存，并按 capture epoch 拒收旧授权下的在途结果。旧请求完成不能删除新授权下的同 key 请求。
- 修复证据质量基准的空集合判断：本应有依据的笔记若被错误标记，现在会使基准失败；报告显式计数 false positives 和 false negatives。

## 本机证据

可靠性报告：build/batch23-verified/report.json，绑定 4079c2c，source_unchanged=true，10/10 pass。测试期间使用独立 build 数据目录；既有 build/calm-ui-data 未用于验收。该运行使用报告所列本机依赖，不能冒充更新后 Windows lock 的全新安装环境。

| 检查 | 结果 | 限定范围 |
| --- | --- | --- |
| 5 分钟媒体 / 完整任务 | pass / pass | 合成输入、固定字幕和摘要 |
| 30 分钟媒体 / 完整任务 | pass / pass | 同上 |
| 60 分钟媒体 / 完整任务 | pass / pass | 同上；历史 60 分钟失败不再在该 fixture 路径复现 |
| 180 分钟媒体 / 完整任务 | pass / pass | 完整门禁耗时 160.047 秒，输入时长为 180 分钟 |
| 5 个混合队列回调 | pass | 单工作者、在途重复 enqueue、完成日志重开；不是全部崩溃恢复场景 |
| 取消任务 | pass | 最终 cancelled；现有 cancel_latency_seconds 从任务启动计时，不能用作取消请求后 ≤2 秒的证明 |
| 后端全量 | 588/588 | 隔离目录 build/batch23-tests-final/data |
| 脚本全量 | 62/62 | 含误报/漏报评分回归 |
| 扩展 + Web | 73 个测试文件通过 | 包括撤权重授、旧请求晚返回竞态；不是原生授权弹窗实机矩阵 |
| 架构 / 工作流 shell / i18n | pass | 现有边界与资源覆盖检查 |

第一次后端全量为 587/588：旧健康检查测试要求目录以 data/tasks 结尾；使用上述隔离 data 目录完整重跑为 588/588。未通过修改业务实现或放宽断言消除该环境差异。

## 尚不能关闭的条目

- #130/#131/#132：仍需重型/下载/字幕独立预算、真实进程退出、残留 lease、多进程上传、低磁盘和重启恢复矩阵；现有单元测试不能替代全部实机场景。
- #134：当前提交的可靠性执行已接入发布前门禁；半完成 draft、同 tag 重跑和公开资产不可变的端到端演练尚未执行。
- #135：撤权字幕竞态已有执行式自动化证据；Chrome/Edge 原生允许、拒绝、撤销、重新授权矩阵仍待实机。
- #129：原有 53 个基准样本包含大量重复模板；修复评分不代表具备 50 个独立的中英文实体、数字、因果、步骤、释义样本。仍需扩充语义覆盖。
- #150/#152：已有编码/结构回归通过；所有解码入口、原始字节重解码、正文/目录/导出共享结构的完整审计尚未完成。

因此本次不关闭上述产品 issue，不执行商店提交、正式发布或签名配置。
