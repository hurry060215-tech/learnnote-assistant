# 扩展与本地工作台连接

扩展发送当前页面内容，本机 LearnNote App 提供处理服务，工作台可以在浏览器中使用。

- 打开客户端：服务已运行时打开或复用网页工作台；离线时尝试 learnnote://open 启动本地 App。
- 安装器为当前 Windows 用户注册协议，只启动固定的 LearnNote.exe，不把网址参数当作命令执行。
- App 在指定端口发现兼容的已运行服务时，打开工作台并退出本次启动，避免重复实例。
- 扩展依次检查保存地址、当前浏览器已打开的本机标签页地址和默认 8765；只接受带版本与协议标记的服务，不扫描任意端口。
- 首次协议唤起可能需要浏览器确认；尚未安装协议处理器时，需要先启动 App。网页本身无法直接启动任意本地程序。
- B 站首页推荐卡片不作为任务来源，请打开具体视频页面。未取得浏览器字幕不等于平台无字幕，发送后的标准处理仍先探测平台字幕。
- 已加载的解压扩展修改源码后，需要在浏览器扩展管理页点击重新加载。

验证：49 项扩展测试、25 项桌面测试通过；实际打包 EXE 可启动本地服务，第二次启动复用服务并退出。商店安装与浏览器协议确认弹窗未纳入自动通过结论。
