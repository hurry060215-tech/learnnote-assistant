# 真实视频与 DS 验收记录

用户明确指定 B 站 BV1kbb56DEkM，以及 LearnNote 已配置的 deepseek-flash。数据目录独立于原有资料库；没有读取浏览器 Cookie，没有把 API Key 写入报告或任务 JSON。

## 运行结果

| 项目 | 已观察结果 |
| --- | --- |
| 输入时长 | 2311.146 秒，约 38 分 31 秒 |
| 公开字幕获取 | 未取得可用字幕，字幕模式停止，没有伪造正文 |
| 正常媒体下载 | 成功，下载阶段约 117.421 秒 |
| 首次整段 ASR | 失败：mkl_malloc 内存分配错误；最后记录进度 19:38，进程 RSS 采样峰值 2,106,048,512 bytes |
| 修复后的恢复任务 | 354c7aeaa58f，复用已下载媒体，状态 success，summary_source=text-llm |
| 恢复使用的代码 | 4d3edf0；对应 PR #197 的后续差异为文档更新，运行期间后端代码未变 |
| 转写 | Whisper small，五分钟窗口，8 个检查点，1256 个片段，10,328 字符 |
| 恢复阶段耗时 | 547.219 秒；其中 transcript 阶段 466.875 秒，summary 阶段 21.359 秒 |
| 恢复进程 RSS 采样峰值 | 1,182,158,848 bytes；不是整个进程树的峰值 |
| DS 生成用量 | 2 次请求，输入 35,424 / 输出 4,395 / 合计 39,819 tokens |
| 连通性探测 | 独立的小请求通过，另用 23 tokens |

首次失败任务 af96dee4aa2e 和下载媒体均保留。分块算法验证了失败后已完成窗口可复用、模型变化失效、损坏缓存重算和绝对时间偏移；此处真实恢复首次使用新检查点，因此重新转写了音频，没有声称复用了旧算法未保存的转写内容。

## 输出核对和边界

- 原模型输出意外添加自测章节，而用户选项 generate_questions=false。已补生成后偏好约束，保留原稿历史版本，再按该约束整理验收正文；没有为此再次调用模型。
- 原稿历史版本：2cbda4c46cbab90cd4812db9e4ec47ddf5d8c8c457c072538e216d5e764199ff。
- 整理后的 75 条投影中：38 条只有候选出处、2 条推断、35 条待核对、0 条被规则认定为完整原文直接引用。这不是语义正确率，生成成功也不等于内容已逐条核实。
- 修复了 claims HTTP 接口错误返回 null，以及中文整句作为单个 token 导致大量候选定位缺失的问题。重建接口只计算来源映射，不再请求模型。
- 阅读页和 HTML/DOCX/PDF/旧 Markdown 导出均明确提示 AI 草稿需要来源核对。额外核对提示不再造成导出重复标题。
- 最终四页 PDF 已逐页查看，没有明显裁切、重叠或替换字符；渲染工具记录 Symbol/ArialUnicode 的字体查找提示，页面实际中文可见。DOCX 只做结构检查；自动渲染缺少 LibreOffice，Word/WPS 原生分页仍待用户核对。

本地证据位置：build/live-ds-20260920/handoff-data/tasks/354c7aeaa58f 和 build/live-ds-20260920/review-output。原有 build/calm-ui-data 没有用于存放此次任务。

## 尚未完成的范围

本轮不是 31 个 issue 的全部收口：原生权限矩阵、Google 商店当前状态/普通安装、Word/WPS、macOS 原生流程，以及广泛的语义正确率、p95 时延、多模态 OCR 覆盖、完整迁移矩阵、动态双语和跨来源语义验收仍需推进。不能将这些未完成工程统一归因于用户凭据。
