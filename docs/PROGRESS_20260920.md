# 剩余工作推进记录

## 已合并或本批完成

- PR #192 已合并：轻重队列独立预算与执行，跨实例重复任务观察，本地字幕恢复身份，跨进程上传预算，进程退出后的预留回收，媒体子进程取消，真实发布脚本的离线 draft 恢复演练。对应提交 bcfc16c。
- PR #192 的 Windows CI、CodeQL、依赖审查、Windows/macOS/Linux 契约测试和 UI visual acceptance 均通过。macOS 契约测试不计为 macOS 原生客户端验收。
- 本地 build/remaining-verified/report.json 绑定 4611012，10/10 pass，source_unchanged=true。该矩阵包括 5/30/60/180 分钟媒体与完整管线、混合队列和取消。
- PR #193 已通过全部检查并合并：新增 80 项双语来源投影检查，修复小数、比例、否定子串和代码块投影。具体边界见 EVIDENCE_CORPUS_20260920.md。
- Chrome 提交工作流仅提供 Google；本批按官方 v2 API 补上传状态检查和审核后暂存。缺少凭据的 preflight 不发起网络请求。
- build/chrome-store-ready/LearnNote-Browser-Extension.zip 通过 17 文件及版本校验。它是当前代码的验收候选包，不替换 v0.2.13 的公开资产；正式发布前需完成版本/资产一致性处理。
- scripts/export-long-document-qa.py 通过实际导出接口生成 35 页中英文样例，自动检查首尾章节、40 个可编辑表格、代码、来源链接和替换字符。PDF 全部页面已生成 PNG，仅检查了代表页；整体视觉验收、Word/WPS 仍待完成。

## 对已列缺口的更新

- PR #195：实际工作台事件重放去重及旧 claim 缓存重新核对，已合并。PR #196：已有数据副本的重复迁移/恢复审计，已合并；样本包含历史验收数据，不能视作全面业务资料覆盖。
- 用户提供的 38 分 31 秒 B 站视频，在真实整段 Whisper 转写中出现 MKL 内存分配失败（进程 RSS 峰值约 2.1 GB）。本批增加五分钟窗口和原音频/模型绑定检查点；相关 161 项测试通过，真实媒体恢复仍需以最终任务报告为准。此前合成媒体 gate 不覆盖此类真实 ASR 分配问题。
- 用户已指定现有 deepseek-flash 配置，真实连通性检查通过；不再把“缺少模型选择”列为外部阻塞。

- 队列独立预算、跨进程上传预算、取消请求计时、发布恢复模拟已从“未实现”转为有执行证据。
- Chrome 原生授权、真实登录态、真实 ASR/模型、完整进程树资源、Word/WPS 与 macOS 原生流程仍没有完整证据。
- 80 项双语投影断言不能代替真实语义蕴含评估；合法释义的验证覆盖仍需扩充。
- 第 4–8 批的已有功能通过相应自动化回归，不等于原始 issue 的全部验收标准完成。模型 SLO、完整迁移恢复矩阵、批注同步、社区显式采样、动态双语和跨来源语义能力仍需逐项核验/补齐。

本记录不关闭上述产品 issue，不把剩余开发工作转嫁给用户。仅真正需要用户条件的事项列在 USER_ACTIONS_20260920.md。
